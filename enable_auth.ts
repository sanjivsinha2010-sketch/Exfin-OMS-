// We cannot enable email/password auth programmatically via Client SDK.
// We must either use an alternative or the user must enable it.
console.log("Email/Password auth is disabled in the Firebase project.");
